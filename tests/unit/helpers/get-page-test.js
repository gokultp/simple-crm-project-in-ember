import { getPage } from 'crm/helpers/get-page';
import { module, test } from 'qunit';

module('Unit | Helper | get page');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPage([42]);
  assert.ok(result);
});
