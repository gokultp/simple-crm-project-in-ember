import { header } from 'crm/helpers/header';
import { module, test } from 'qunit';

module('Unit | Helper | header');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = header([42]);
  assert.ok(result);
});
